import { Schema, Type } from '@google/genai';

/**
 * Schema definition for Context structured output with Google GenAI.
 */
export const ContextSchema: Schema = {
  type: Type.OBJECT,
  properties: {},
  required: []
};
